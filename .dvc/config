[core]
    remote = s3remote
    autostage = true

[remote "s3remote"]
    url = s3://authentiguard-datasets
    endpointurl = ${S3_ENDPOINT_URL}
    access_key_id = ${AWS_ACCESS_KEY_ID}
    secret_access_key = ${AWS_SECRET_ACCESS_KEY}
